import 'package:flutter/material.dart';

class LinkInputScreen extends StatelessWidget {
  const LinkInputScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Link Input'),
      ),
    );
  }
}
