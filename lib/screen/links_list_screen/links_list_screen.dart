import 'package:flutter/material.dart';

class LinksListScreen extends StatelessWidget {
  const LinksListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Links List'),
    );
  }
}
